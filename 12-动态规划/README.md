# 第 12 章：动态规划

本章已扩展为 [HTML 详细讲义](./index.html)：保留 6 个主节，新增 40 个关键问答、12 张状态与推演表格、15 份补充 C++17 示例（含综合测试）。

## 详细阅读路线

1. **线性 DP**：结尾状态与全局答案、最大子数组下标恢复、环形劫舍、记忆化与滚动递推。
2. **0/1 背包**：上一行依赖、倒序原因、恰好装满与不可达状态、等和分割及目标和推导。
3. **完全背包**：最少硬币与方案、组合和有序序列的区别、重复面值、零值与模数语义。
4. **序列 DP**：LCS 回溯、一维编辑距离的旧左上角、LIS 最小结尾与真实前驱链。
5. **区间 DP**：回文子序列和子串对照、合法计算顺序、戳气球的最后一步与固定边界。
6. **树形与状态机**：条件返回值、显式后序、最多 K 次交易、状态压缩任务分配及方案恢复。

内容与代码由 `detail.json` 统一生成。`examples/detail-dp-tests.cpp` 提供 100 轮小输入对照、方案性质检查、固定边界和深链测试；引用同目录 14 份补充实现与 6 份基础模板。

在本章目录执行（请保留完整 `examples` 目录，只编译入口文件）：

```powershell
g++ -std=c++17 -O2 -Wall -Wextra examples/detail-dp-tests.cpp -o dp-tests
.\dp-tests.exe
```

不要定义 `NDEBUG`。当前环境没有可用 C++ 编译器，测试源码尚未编译运行；页面与源文件一致性检查不等同于算法运行测试。

## 动态规划五步法

1. 定义状态：`dp[...]` 精确表示什么。
2. 写出转移：当前状态由哪些更小状态得到。
3. 确定初始条件：最小问题的答案是什么。
4. 确定计算顺序：依赖状态必须先算出。
5. 明确最终答案：不一定总是最后一个状态。

动态规划适用于存在重叠子问题和最优子结构的问题。状态设计比代码更重要。

## 记忆化搜索与递推

- 记忆化搜索：从目标状态向下递归，只计算会访问到的状态，易于从暴力搜索改造。
- 自底向上递推：控制计算顺序，无递归栈，通常常数更小。
- 两者状态和转移本质相同，不要把“用了递归”误认为不是 DP。

## 高频类型

### 线性 DP

- 打家劫舍：`dp[i]` 表示考虑前 `i` 个位置的最优值。
- 最大子数组和：以当前位置结尾的最优和，要么单独开始，要么接在前一段后。
- 股票问题：用“天数、交易次数、是否持有”描述状态。

### 背包 DP

- 0/1 背包：每件物品最多一次，压缩为一维后容量必须倒序遍历。
- 完全背包：每件物品可无限次，容量通常正序遍历。
- 组合数与排列数：外层遍历物品还是容量会影响计数顺序含义。
- 常见变形：分割等和子集、零钱兑换、目标和、单词拆分。

### 序列 DP

- 最长递增子序列：`O(n²)` DP；也可用贪心 + 二分优化到 `O(n log n)`。
- 最长公共子序列：二维状态表示两个前缀的答案。
- 编辑距离：插入、删除、替换对应三种前驱状态。

### 区间 DP

状态常为 `dp[l][r]`，按区间长度从小到大计算。典型题有最长回文子序列、戳气球、石子合并。

### 树形 DP

以节点为状态载体，由孩子状态合并出父节点状态。典型题有树上打家劫舍、树的最大独立集。

### 状态压缩 DP

用位掩码表示已选集合，适合节点数较少的旅行商、任务分配和集合匹配问题，复杂度常含 `2^n`。

## 空间优化

- 若当前行只依赖上一行，可使用滚动数组。
- 一维压缩时必须确认遍历方向，避免在同一轮错误复用刚更新的值。
- 优化前先写出正确的二维状态和转移，再压缩空间。

## 易错点与自测

- [ ] 状态定义包含“范围、限制和结尾条件”。
- [ ] 初始化没有把不可达状态误设为 0。
- [ ] 最大值问题中的负无穷、最小值问题中的正无穷设置安全。
- [ ] 能解释 0/1 背包为什么倒序、完全背包为什么正序。
- [ ] 能独立写出最大子数组和、LCS、编辑距离和零钱兑换。
- [ ] 能区分组合、排列和物品身份，解释 `dp[0]=1` 的空方案含义。
- [ ] 能说明滚动数组为何可能破坏方案恢复，以及 LIS 的 `tails` 为何不是实际子序列。
- [ ] 能解释戳气球为什么枚举最后一步，树形 DP 为什么返回条件状态。
- [ ] 能按状态数与转移代价估算复杂度，识别伪多项式和指数规模。
- [ ] 能同时验证恢复方案的合法性与最优值，而非只比对一个标准答案。
